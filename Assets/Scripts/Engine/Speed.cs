public enum Speed
{
    // Game is paused
    Paused,

    // 1 second == 1 in-game hour
    Normal,

    // 0.5 second == 1 in-game hour
    Faster,

    // 0.2 second == 1 in-game hour
    Fastest
}